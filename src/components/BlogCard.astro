---
import type { CollectionEntry } from "astro:content";
import SecondaryHeadline from "@components/SecondaryHeadline.astro";
import TagList from "@components/TagList.astro";

interface Props {
  post: CollectionEntry<"blog">;
}

const { post } = Astro.props;
---

<article class="bg-navy-200 h-full">
  <a
    href={`/blog/${post.slug}`}
    class="flex flex-col gap-0.75 h-full border border-gray-dark hover:border-yellow transition-colors bg-black group"
  >
    <SecondaryHeadline text={post.data.title} />
    <div
      class="flex flex-col gap-4 h-full p-4 border-t border-solid border-gray bg-navy-200"
    >
      <time
        datetime={post.data.pubDate.toISOString()}
        class="text-lg text-yellow text-shadow-black block"
      >
        {
          post.data.pubDate.toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric",
            timeZone: "America/New_York",
          })
        }
      </time>
      <p class="text-gray-300">{post.data.description}</p>
      <TagList tags={post.data.tags} />
    </div>
  </a>
</article>
