---
interface Props {
  class?: string;
  background?: "navy-100" | "navy-200";
}

const { class: className, background = "navy-200" } = Astro.props;
---

<div
  class={`p-4 bg-${background} outline outline-gray -outline-offset-1 ${className || ""}`}
>
  <slot />
</div>
