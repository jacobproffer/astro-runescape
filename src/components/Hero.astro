---
interface Props {
  headline: string;
}

const { headline } = Astro.props;
---

<section>
  {
    headline && (
      <h1 class="font-medium text-6xl text-yellow font-family-cinzel text-balance">
        {headline}
      </h1>
    )
  }
  <slot />
</section>
